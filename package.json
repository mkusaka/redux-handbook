{
  "name": "redux-handbook",
  "version": "1.0.0",
  "description": "An opionated way to write applications with Redux.",
  "homepage": "http://bpxl-labs.github.io/redux-handbook/",
  "repository": {
    "type": "git",
    "url": "git://github.com/bpxl-labs/redux-handbook.git"
  },
  "scripts": {
    "watch": "honkit serve",
    "build": "rm -rf _book && honkit build",
    "deploy": "yarn run build && yarn gh-pages -d _book"
  },
  "author": "Black Pixel",
  "license": "MIT",
  "dependencies": {
    "gitbook-plugin-anchors": "^0.7.1",
    "gitbook-plugin-image-captions": "^3.1.0",
    "gitbook-plugin-prism": "^2.4.0"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "honkit": "^3.4.0"
  }
}
